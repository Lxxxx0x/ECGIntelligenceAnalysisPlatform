# Frontend request base URL (build-time for Vite)
VITE_API_URL=/api

# Vite dev-server proxy target
VITE_PROXY_TARGET=http://localhost:8080

# Frontend host port for Docker Compose
FRONTEND_PORT=8080

# Runtime backend target for Docker Nginx proxy
BACKEND_URL=http://your-backend-host:8080
